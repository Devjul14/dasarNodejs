// const fs = require('fs');

// // membuat obj stream u/baca
// //var rs = fs.createReadStream(`data1.txt`);

// //buat obj stream u/ nulis file
// var ws = fs.createReadStream(`data1.txt`).pipe(fs.createWriteStream(`data4.txt`));

// // menyalin file ke data1 ke data4 
// //rs.pipe(ws);
// console.log(`lagi nyalin`);
