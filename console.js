console.info(`Program Mulai`)
console.time(`Waktu eksekusi`)

// proses yang diukur waktunya
for (let index = 0; index < 1000000; index++) {
    // tidak ada eksekusi
    console.log(`Waktu eksekusi: `)
}

console.info(`program selesai`)