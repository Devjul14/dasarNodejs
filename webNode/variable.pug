<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        h2 variable dalam pug
        ul(type="none")
            - var i=0
            while i < data.length
                if i % 2 == 1
                    - var s = data[i++].toUppercase()
                else
                    - var s = data[i++]
                li=s